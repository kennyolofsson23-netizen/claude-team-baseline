# Change Wish — `<short-title>`

- **Filed by**: `<your-name>`
- **Filed on**: `<YYYY-MM-DD>`
- **Urgency**: `<none | blocks-current-work | critical>`
- **Affects**: `<claude-team-baseline | specific-product | all-products | process/X>`

## What change

<One or two sentences — the actual change being proposed.>

## Why

<The problem this solves. What happens today without this change? Who feels the pain?>

## Alternatives considered

<At least two. Why were they rejected?>

1. **Alternative A**: …
   Rejected because: …

2. **Alternative B**: …
   Rejected because: …

## Cost / impact

- **Cost**: `<one-off / ongoing / free>` — estimate if billable
- **Maintenance burden**: `<low / medium / high>`
- **Risks introduced**: `<list>`
- **Rollback plan**: `<how we undo this if it doesn't work>`

## Open questions

<Things the ARB should decide or point you at the right person for.>

---

## ARB decision (filled by ARB)

- **Decision**: `<approved | approved-deferred | declined | needs-more-info>`
- **Decided on**: `<YYYY-MM-DD>`
- **Decided by**: `<names>`
- **Rationale**: `<one paragraph>`
- **Next step**: `<PR by whom, by when / reason closed>`
