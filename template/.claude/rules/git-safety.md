# Git Safety Rules (always loaded)
- Feature branches always — never push directly to main
- Never push to main unless explicitly told
- Never commit .env, .png, credentials, or secrets (enforced by hooks)
- Default context is local dev — never target production unless explicitly told
- Confirm before destructive operations
