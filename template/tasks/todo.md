# TODO

Add work items here. Claude reads this at session start.

## Format
- [ ] Short description of the task
  - Optional sub-bullet with detail
  - Acceptance: how we know it's done

## Example
- [ ] Add a "Hello" page that reads one row from the `customers` table
  - Acceptance: navigating to `/hello` shows the first customer's name; test covers happy path + empty-table case
